<%= form.label :body %>
<%= react_component('Editor', {
  markdown: form.object.body,
  id: "#{form.object_name}_body",
  nameAttr: "#{form.object_name}[body]"
}) %>
<%= render 'articles/markdown_help' %>
