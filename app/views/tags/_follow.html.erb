<% if current_user.follow?(tag) %>
  <%= link_to tc(:unfollow), unfollow_tag_path(tag), method: :post, class: "follow-link", remote: true %>
<% else %>
  <%= link_to tc(:follow), follow_tag_path(tag), method: :post, class: "follow-link", remote: true %>
<% end %>
